version: '3.8'

services:
  web:
    build: .
    container_name: home_organization
    ports:
      - '8080:80'
    volumes:
      - .:/var/www/html
    env_file:
      - .env
    environment:
      - APP_ENV=local
